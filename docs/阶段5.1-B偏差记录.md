# 阶段5.1-B 偏差记录

日期：2026-08-02    依据：AGENTS.md 第一性原理审查

## 一、偏差事实

阶段5.1-B 的计划边界为“建立Evaluation体系，并完成规则基线”。

当前实际新增了不属于本阶段目标的文件：

- evaluation/run_comparison.py（规则 vs 模拟LLM vs 混合对比评测）
- tests/test_comparison.py

“模拟LLM对比”属于阶段5.2内容，提前出现在5.1-B，构成阶段边界偏移。

## 二、偏离原因

1. 将“直接跑完这次”理解为可提前执行对比评测，未严格按阶段边界执行；
2. 未先输出“阶段边界确认”即进入对比实现，违反“新增优先、每步确认”原则；
3. 对“模拟LLM”的定位（流程验证工具 vs 阶段交付物）判断不严谨。

## 三、是否影响架构

不影响架构。

- 未修改任何现有代码与Workflow；
- run_comparison.py / test_comparison.py 为独立新增文件；
- 规则基线（evaluation/reports/translator_rule_baseline_v1.0.*）已正常产出，属于5.1-B合法成果；
- 未接入Router/Adapter，未触发任何LLM调用。

## 四、是否保留该代码

建议保留，但标记为“阶段5.2预留”，不计入5.1-B验收范围。

理由：文件为独立评测脚本，不影响现有流程；直接删除浪费已完成的框架代码。最终保留与否由人工确认。

## 五、后续真实 DeepSeek 接入时如何复用

1. 复用评测集与指标：evaluation/cases/translator_cases.json、evaluation/metrics.py 不依赖模型；
2. 复用三模式框架：run_comparison.py 的 rule / llm / hybrid 对比结构不变；
3. 替换模型源：将 simulate_llm() 替换为 models/deepseek.py 的 DeepSeekAdapter.generate()；
4. Prompt 绑定：对比报告记录 prompt_version（prompts/manifest.yaml），保证可回归；
5. 接入条件：真实API可用、成本受控、通过评测指标后才允许上线。

## 六、纠正措施

1. 暂停运行模拟LLM对比；
2. 阶段5.1-B 以“规则基线评测”为收口；
3. 阶段5.2 待人工确认后，以真实 DeepSeek 实施；
4. 后续阶段严格执行“阶段边界确认”后再新增实现。

本记录仅作偏差登记，未删除或修改任何文件。
