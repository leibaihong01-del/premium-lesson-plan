# ADR-002：建立 Document IR 中间层

- 状态：Frozen
- 日期：2026-08-03

## 决策

所有 Word 输入必须经过：

```text
DOCX -> Clean -> Document IR -> Builder -> DOCX
```

Document IR 只保存内容、结构、语义，不保存 font/size/color/style/xml。

## 原因

解除“内容 = 格式”绑定，避免 Word 直接到 Word 的污染链路。
