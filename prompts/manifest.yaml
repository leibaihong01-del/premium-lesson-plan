# Prompt 资产清单（阶段5.1-A）
# 版本管理：每个 prompt 独立版本号；变更必须升版本并记录原因。
version: "1.0"
updated_at: "2026-08-02"

prompts:
  - id: translator.system
    version: "1.0"
    purpose: "需求转译系统角色：定义课程建设需求转译助手行为与输出契约"
    path: "translator/system.md"
    model_compat: ["deepseek", "glm", "mimo"]
    change_log: ["初始建立"]

  - id: translator.user_template
    version: "1.0"
    purpose: "需求转译用户模板：将自然语言与用户画像格式化为结构化JSON"
    path: "translator/user_template.json"
    model_compat: ["deepseek", "glm", "mimo"]
    change_log: ["初始建立"]
