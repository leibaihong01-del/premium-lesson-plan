# 自主质量闸门与一次性报告规则

版本：1.0
适用范围：所有文档生成、修复、验收任务。

## 一、原则

1. 用户不应参与低级验证循环；
2. Agent 必须在交付前完成自主质量检查，并一次性报告所有问题；
3. 禁止在未完成完整扫描前逐个向用户确认。

## 二、执行要求

- 交付前完成全量扫描与自检；
- 一次输出完整问题清单（含位置、类型、状态、是否需修复）；
- 仅重大方向决策才请求用户确认；
- 低层级逐项确认属于禁止行为。

## 三、验收

- 交付报告包含全部已发现问题的处理状态；
- 非致命差异必须记录，不得隐藏；
- 未完成全量扫描前不得宣称完成。
